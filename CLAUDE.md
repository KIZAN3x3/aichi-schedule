# 愛知活動スケジュール帳 - 設計仕様書

## 概要
愛知県内の政治活動スケジュールを、16支部単位で共有・管理するアプリ。
既存アプリ「選対スケジュール帳（sentaicho）」「banner-maker-v2」の構成思想を踏襲しつつ、新規に構築する。

対象ユーザー：約200人（支部間の垣根なく誰でも投稿・参加可能）

---

## 技術構成

- **フロントエンド**：素のHTML+JS（ビルド不要、Vite/React等は使わない）
- **ホスティング**：Vercel
- **API**：Vercel Serverless Functions（`api/*.js`）
- **DB／ストレージ**：Supabase（新規プロジェクト、laiton-shopとは分離、無料枠で運用）
  - Supabase Realtimeで自動反映（他端末にもリアルタイムで更新が届く）
  - Supabase Storageで備品の画像を保存
- **認証**：パスワード入力欄1つのみ。入力値で自動判定
  - `123` → 一般ユーザー権限
  - `123123` → マスター管理者権限
  - （banner-maker-v2と同じパスワード体系）

---

## 機能① 支部スケジュール（支部ごとに完全分離）

### 導線
1. 支部を選択（16支部からプルダウン）
2. カレンダーで日付を選択
3. その支部・その日の予定を時間軸に沿って一覧表示

- 選んだ支部・日付の予定のみ表示（他支部の予定は見えない）
- 1日につき何件でも予定を登録可能（例：朝の街頭活動、昼の訪問、夜の会合など、時間帯ごとに場所・活動内容が異なってOK）

### データ構造

**`events`テーブル**
| カラム名 | 型 | 説明 |
|---|---|---|
| id | uuid/serial | 主キー |
| branch | text | 支部名（16支部のいずれか） |
| date | date | 活動日 |
| time | time | 活動時間 |
| place | text | 場所（自由記述） |
| content | text | 活動内容（自由記述） |
| poster_name | text | 投稿者名（自己申告） |
| created_at | timestamp | 作成日時 |

**`participants`テーブル**
| カラム名 | 型 | 説明 |
|---|---|---|
| id | uuid/serial | 主キー |
| event_id | uuid/serial | events.idへの外部キー |
| participant_name | text | 参加者名（自己申告） |
| created_at | timestamp | 作成日時 |

### 権限
- 一般ユーザー：投稿可能。**自分の投稿（poster_nameが自申告名と一致するもの）のみ**編集・削除可
- マスター管理者：**全投稿**を編集・削除可

### 画面要素
- 予定一覧（時間軸順）：時間／場所／活動内容／投稿者
- 「参加する」ボタン＋参加者一覧
- 新規投稿フォーム：支部（プルダウン）／日付（カレンダー）／時間／場所（自由記述）／活動内容（自由記述）／投稿者名

---

## 機能② 備品管理チェック（全体共通・支部の区別なし）

支部分けはせず、全体で1つの備品リストを共有する。

### データ構造

**`equipment`テーブル**
| カラム名 | 型 | 説明 |
|---|---|---|
| id | uuid/serial | 主キー |
| item_name | text | 品目名 |
| location | text | 保管場所（自由記述） |
| image_url | text | 画像URL（Supabase Storage） |
| memo | text | メモ |
| updated_by | text | 最終更新者名（自己申告） |
| updated_at | timestamp | 更新日時 |

### 権限
- 一般ユーザー・マスター管理者ともに**同一権限**で自由に新規登録・場所更新・画像追加・編集が可能

### 画面要素
- 備品一覧（画像サムネイル付き）
- 新規登録フォーム：品目名／保管場所／画像アップロード／メモ

---

## デザイン
- イメージカラーは既存アプリ（banner-maker-v2等）と合わせ、橙色グラデーション系を基本とする
  - 参考：`#f5871f` → `#f06e15` → `#e0590a`

---

## 備考・参考にした既存アプリ
- **sentaicho**（選対スケジュール帳）：React + Node.js API（ConoHa VPS）。都道府県→選挙区→市区町村の階層構造、3段階認証（super-admin/admin/user）の考え方を参考にした
- **banner-maker-v2**：素のHTML+JS＋Vercelのデプロイフロー、パスワード体系（123／123123）を踏襲
- **支援者名簿リーダー**：同様の軽量デプロイ構成（index.html + api/*.js）
