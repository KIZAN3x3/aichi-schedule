# 愛知活動スケジュール＆備品管理 - 設計仕様書

## 概要
愛知県内の政治活動スケジュールを、支部単位（西県連・東県連 + 1〜16支部の18支部）で共有・管理するアプリ。
既存アプリ「選対スケジュール帳（sentaicho）」「banner-maker-v2」の構成思想を踏襲しつつ、新規に構築する。

対象ユーザー：約200人（支部間の垣根なく誰でも投稿・参加可能）

---

## 技術構成

- **フロントエンド**：素のHTML+JS（ビルド不要、Vite/React等は使わない）
- **ホスティング**：Vercel
- **API**：Vercel Serverless Functions（`api/*.js`）
- **DB／ストレージ**：Supabase（新規プロジェクト、laiton-shopとは分離、無料枠で運用）
  - Supabase Realtimeで自動反映（他端末にもリアルタイムで更新が届く）
  - Supabase Storageで備品の画像を保存
- **認証**：パスワード入力欄1つのみ。入力値で自動判定
  - `123` → 一般ユーザー権限
  - `123123` → マスター管理者権限
  - （banner-maker-v2と同じパスワード体系）

---

## 機能① 支部スケジュール（支部ごとに完全分離）

### 導線
1. 支部を選択（西県連・東県連 + 1〜16支部の18択からプルダウン）
2. カレンダーで日付を選択
3. その支部・その日の予定を時間軸に沿って一覧表示

- 選んだ支部・日付の予定のみ表示（他支部の予定は見えない）
- 1日につき何件でも予定を登録可能（例：朝の街頭活動、昼の訪問、夜の会合など、時間帯ごとに場所・活動内容が異なってOK）

### データ構造

**`events`テーブル**
| カラム名 | 型 | 説明 |
|---|---|---|
| id | uuid/serial | 主キー |
| branch | text | 支部名（西県連・東県連 + 1〜16支部の18択のいずれか） |
| date | date | 活動日 |
| time | time | 活動時間 |
| place | text | 場所（自由記述） |
| content | text | 活動内容（自由記述） |
| poster_name | text | 投稿者名（自己申告） |
| created_at | timestamp | 作成日時 |

**`participants`テーブル**
| カラム名 | 型 | 説明 |
|---|---|---|
| id | uuid/serial | 主キー |
| event_id | uuid/serial | events.idへの外部キー |
| participant_name | text | 参加者名（自己申告） |
| created_at | timestamp | 作成日時 |

### 権限
- 一般ユーザー：投稿可能。**自分の投稿（poster_nameが自申告名と一致するもの）のみ**編集・削除可
- マスター管理者：**全投稿**を編集・削除可

### 画面要素
- 予定一覧（時間軸順）：時間／場所／活動内容／投稿者
- 「参加する」ボタン＋参加者一覧
- 新規投稿フォーム：支部（プルダウン）／日付（カレンダー）／時間／場所（自由記述）／活動内容（自由記述）／投稿者名

---

## 機能② 備品管理チェック（全体共通・支部の区別なし）

支部分けはせず、全体で1つの備品リストを共有する。

### データ構造

**`equipment`テーブル**
| カラム名 | 型 | 説明 |
|---|---|---|
| id | uuid/serial | 主キー |
| item_name | text | 品目名 |
| location | text | 保管場所（自由記述） |
| image_url | text | 画像URL（Supabase Storage） |
| memo | text | メモ |
| updated_by | text | 最終更新者名（自己申告） |
| updated_at | timestamp | 更新日時 |

### 権限
- 一般ユーザー・マスター管理者ともに**同一権限**で自由に新規登録・場所更新・画像追加・編集が可能

### 画面要素
- 備品一覧（画像サムネイル付き）
- 新規登録フォーム：品目名／保管場所／画像アップロード／メモ

---

## 機能③ 日程調整（支部ごと、調整さんに近いUI）

候補日時を複数出し、回答者が〇（参加できる）／△（未定）／✕（参加できない）で回答を集めてから、作成者本人またはマスター管理者が候補を1つ選んで決定する。決定すると、その候補日時がそのまま機能①の予定として登録される。

### 導線
1. 支部を選択（機能①と同じ支部・同じ保存先を共有。選んだ支部は他画面と引き継がれる）
2. 日程調整の一覧を確認、または「＋ 日程調整を作成」で新規作成（タイトル／候補日時複数／場所／内容／回答締切／作成者名）
3. 回答者が候補ごとに〇△✕とコメントで回答（代理登録も可）
4. 作成者本人またはマスター管理者が、候補×回答者の表から候補を1つ選んで「決定」

### データ構造

**`coordinations`テーブル**
| カラム名 | 型 | 説明 |
|---|---|---|
| id | uuid | 主キー |
| branch | text | 支部名（西県連・東県連 + 1〜16支部の18択） |
| title | text | タイトル |
| place | text | 場所（自由記述） |
| content | text | 内容（自由記述） |
| created_by | text | 作成者名（自己申告） |
| reply_deadline | date | 回答締切（任意） |
| status | text | open=調整中 / decided=決定済み |
| decided_event_id | uuid | 決定して作られたevents.idへの外部キー（未決定はnull） |
| decided_candidate_id | uuid | 決定した候補（coordination_candidates.id、未決定はnull） |
| decided_at | timestamp | 決定日時 |

**`coordination_candidates`テーブル**（候補日時）
| カラム名 | 型 | 説明 |
|---|---|---|
| id | uuid | 主キー |
| coordination_id | uuid | coordinations.idへの外部キー |
| date | date | 候補日 |
| time | time | 候補時刻（任意。未入力は「終日」扱い） |
| note | text | 補足（任意・50文字以内。例：午前／午後／撮影日） |
| sort_order | integer | 表示順 |

**`coordination_responses`テーブル**（回答者）
| カラム名 | 型 | 説明 |
|---|---|---|
| id | uuid | 主キー |
| coordination_id | uuid | coordinations.idへの外部キー |
| participant_name | text | 回答者名（自己申告） |
| registered_by | text | 代理登録した人の名前（本人登録ならparticipant_nameと同じ） |
| comment | text | コメント（任意・200文字以内） |

**`coordination_answers`テーブル**（候補ごとの回答）
| カラム名 | 型 | 説明 |
|---|---|---|
| id | uuid | 主キー |
| response_id | uuid | coordination_responses.idへの外部キー |
| candidate_id | uuid | coordination_candidates.idへの外部キー |
| mark | text | yes=〇 / maybe=△ / no=✕ |

### 権限
- 作成・回答：一般ユーザー・マスター管理者どちらも可
- 回答の編集・取消：**本人（participant_nameが一致）または代理登録した人（registered_byが一致）のみ**
- 決定・削除：**作成者本人（created_byが一致）またはマスター管理者のみ**。削除は調整中・決定済みのどちらでも可能（決定済みを削除しても、機能①の予定と参加者は残る）

### 覚えておくべき仕様
- **決定すると`events`に1件登録される**：候補の日時・調整の場所と内容・決定操作をした人の名前が、そのまま機能①の予定として登録される（`events`にはタイトル列が無いため、決定ダイアログの「内容」欄の初期値に調整のタイトルを自動で含めている）
- **〇（と任意で△）の回答者を、決定時に`participants`へ一括登録できる**（決定ダイアログのチェックボックスで選択。デフォルトは〇のみON）
- **予定を削除すると、調整は自動的に「調整中」に戻る**：`events`側の行が削除されるとDBトリガーが働き、`coordinations`の`status`・`decided_candidate_id`・`decided_event_id`・`decided_at`が自動的にクリアされ、再度決定できるようになる
- **二重決定は起きない**：決定処理はDB関数（`decide_coordination`）が1トランザクションで行い、行ロックにより同時に決定ボタンが押されても予定が2つ作られることはない
- **専用URLで直接開ける**：`coordination.html?id=<coordination_id>`を開くと、対象の支部に画面表示だけ切り替わり（保存済みの支部設定は上書きしない）、その調整の詳細が開く。未ログインで開いた場合はログイン後に同じ画面へ戻る
- **チャットワーク連携**：各調整のカードから、チャットワークの`[info]`記法でまとめた文面（タイトル・候補日・締切・専用URL）または専用URLのみを、クリップボードにコピーできる
- **決定済みの予定側にも導線がある**：日程調整から決定された予定には、機能①の予定カードに元の調整へのリンクが表示される

---

## デザイン
- イメージカラーは既存アプリ（banner-maker-v2等）と合わせ、橙色グラデーション系を基本とする
  - 参考：`#f5871f` → `#f06e15` → `#e0590a`

---

## 備考・参考にした既存アプリ
- **sentaicho**（選対スケジュール帳）：React + Node.js API（ConoHa VPS）。都道府県→選挙区→市区町村の階層構造、3段階認証（super-admin/admin/user）の考え方を参考にした
- **banner-maker-v2**：素のHTML+JS＋Vercelのデプロイフロー、パスワード体系（123／123123）を踏襲
- **支援者名簿リーダー**：同様の軽量デプロイ構成（index.html + api/*.js）
